# Cursor API キー（ローカル台本生成用）
CURSOR_API_KEY=your_api_key_here

# Gemini API キー（GitHub Actions 台本生成用）
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-lite

# 使用モデル（composer-2 が最も低コスト。失敗時は CURSOR_FALLBACK_MODELS を順に試行）
CURSOR_MODEL=composer-2

# fast バリアントは高コストのため、デフォルトは false（standard）
CURSOR_MODEL_FAST=false

# フォールバックモデル（カンマ区切り、任意）
# CURSOR_FALLBACK_MODELS=

# VOICEVOX Engine の URL（デフォルト: ローカル）
VOICEVOX_URL=http://127.0.0.1:50021

# 姉の音量（VOICEVOX volumeScale、デフォルト 1.25）
SPEAKER_VOLUME_ANE=1.25

# 朝ルーティン（scripts/morning_routine.sh）
AUTO_PUSH=true          # 生成後に git push する
SKIP_WEEKEND=true       # 土日はスキップ
